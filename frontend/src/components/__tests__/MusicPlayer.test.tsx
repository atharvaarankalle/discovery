import "@testing-library/jest-dom";
import { describe, expect, it } from "vitest";
import { render } from "@testing-library/react";
import { MusicPlayer } from "../MusicPlayer";

it("tests that vitest is working", () => {
  expect(1 + 1).toEqual(2);
});
